method: bayes
name: hyperparameter-sweep
metric:
  goal: minimize
  name: val_loss
parameters:
  learning_rate:
    min: 0.000001
    max: 0.1
  batch_size:
    values: [8, 16, 32, 64]
  depth:
    min: 1
    max: 4
  embedding_size:
    values: [32, 64, 128]
  n_encoders:
    min: 1
    max: 4